import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Welcome to Mo-Bank</Text>
    </SafeAreaView>
  );
};

export default Home;
